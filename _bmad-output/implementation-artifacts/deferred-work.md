# Deferred Work

- source_spec: `C:\Users\farro\src\k53-guru\_bmad-output\implementation-artifacts\spec-1-1-seed-road-sign-catalog.md`
  summary: The `AddRoadSign` migration exists only for `Migrators.PostgreSQL`; `Migrators.MSSQL` and `Migrators.SqLite` have no equivalent, so `SeedRoadSignsAsync`'s `RoadSigns.AnyAsync()` will throw against those providers, and the app's own `appsettings.json` defaults `DatabaseSettings:DBProvider` to `mssql`.
  evidence: All three independent review layers (blind-hunter, edge-case-hunter, verification-gap) flagged this from different angles, and verification-gap traced the concrete failure path (`HostExtensions.InitializeDatabaseAsync` -> `SeedDataAsync` -> `SeedRoadSignsAsync`) against the default `mssql` config in both `src/Server.UI/appsettings.json` and `tests/Application.IntegrationTests/appsettings.json`. Root cause predates this story: `Migrators.MSSQL` already diverged from `Migrators.PostgreSQL` before this change (it has a `20251011034357_remove_tenantId_role` migration Postgres never received, and `Migrators.SqLite` hasn't been touched since its initial migration), so the three provider migration histories were already out of sync. `dotnet ef migrations add AddRoadSign` also swept unrelated pre-existing drift into the new Postgres migration (dropping FK/columns on `AspNetRoles`, creating a `tenant_users` table with no unique index on `(tenant_id, user_id)`) as a side effect of catching Postgres up. This story's frozen intent explicitly scoped migration work to `Migrators.PostgreSQL` only, and `_bmad-output/implementation-artifacts/epic-1-context.md` documents Postgres as the architecture's migration source of truth — so the code correctly followed its spec; the gap is the pre-existing lack of a cross-provider migration-sync policy plus a stale `mssql` default, not a defect in this story's implementation.

- source_spec: `C:\Users\farro\src\k53-guru\_bmad-output\implementation-artifacts\spec-1-1-seed-road-sign-catalog.md`
  summary: `src/K53Guru/tests/Infrastructure.UnitTests/Services/GeolocationServiceTests.cs` references a `GeolocationService` type that no longer exists anywhere in `src/Infrastructure`, has never compiled, and was silently excluded via `<Compile Remove>` (rather than fixed or deleted) so the new RoadSign tests in the same project could build.
  evidence: Confirmed by the implementation subagent while diagnosing why `Infrastructure.UnitTests` was absent from `K53Guru.slnx` in the first place, and independently re-flagged by both the blind-hunter and verification-gap review layers. The dead file remains in the repo, now permanently skipped rather than resolved.

- source_spec: `C:\Users\farro\src\k53-guru\_bmad-output\implementation-artifacts\spec-1-2-browse-search-sign-catalog.md`
  summary: The `AddRoadSign` migration gap logged against Story 1.1 (Postgres-only, no `Migrators.MSSQL`/`Migrators.SqLite` equivalent) is no longer just a seeding-time risk — Story 1.2 makes `RoadSigns` reachable end-to-end via `/system/roadsigns`, and both the app's default `appsettings.json` and the `Application.IntegrationTests` harness are configured for the `mssql` provider, which still has no `RoadSigns` table.
  evidence: The verification-gap review layer traced the concrete failure: `tests/Application.IntegrationTests/RoadSigns/Queries/RoadSignsPaginationQueryTests.cs`'s `[SetUp]` calls `AddAsync(new RoadSign {...})` against a real MSSQL LocalDB with migrations applied (`Testing.cs:97`), which would fail with an "Invalid object name" error before any assertion runs — confirmed no migration exists anywhere under `Migrators.MSSQL` referencing `RoadSign` (zero grep hits). The substitute coverage added in this diff (`tests/Infrastructure.UnitTests/Features/RoadSigns/RoadSignsQueryHandlerTests.cs`) passes only because it builds its schema via `EnsureCreated()` on SQLite in-memory, bypassing the MSSQL migration path entirely — it would still pass even if the real MSSQL migration were missing, which it currently is. Fixing this (adding `AddRoadSign` migrations for `Migrators.MSSQL`/`Migrators.SqLite`, or switching the default `DBProvider` to `postgresql`) is a cross-cutting decision outside either story's boundaries.

- source_spec: `C:\Users\farro\src\k53-guru\_bmad-output\implementation-artifacts\spec-1-2-browse-search-sign-catalog.md`
  summary: No component-level (e.g. bUnit) tests exist for `RoadSigns.razor` or `RoadSignDetailDialog.razor` — page behavior (search, refresh, dialog wiring, error snackbar) is unverified by anything beyond the underlying query handlers.
  evidence: Confirmed this matches a codebase-wide gap (no bUnit or equivalent Blazor component-test framework is used anywhere in this repo for any existing page, e.g. `Products.razor`/`PicklistSets.razor` have none either), so it's pre-existing technical debt this story inherits rather than introduces, but worth tracking now that a second UI page has shipped with zero UI-level coverage.

- source_spec: `C:\Users\farro\src\k53-guru\_bmad-output\implementation-artifacts\spec-1-3-question-content-model.md`
  summary: The recurring MSSQL/`Migrators.SqLite` migration gap (previously logged against `RoadSign`) now also covers `Question`/`AnswerOption` — no migration exists for either provider, and the app's `appsettings.json` still defaults `DatabaseSettings:DBProvider` to `mssql`.
  evidence: The verification-gap review layer traced the concrete failure: `HostExtensions.InitializeDatabaseAsync` -> `ApplicationDbContextInitializer.InitialiseAsync` -> `Database.MigrateAsync()` runs against whichever provider is configured (`mssql` in both `Server.UI/appsettings.json` and `Application.IntegrationTests/appsettings.json`), and `Migrators.MSSQL`'s newest migration (`20251011034357_remove_tenantId_role`) predates this change — zero grep hits for `Question` anywhere under `Migrators.MSSQL`. The new `QuestionValidationInterceptorTests.cs` passes only because it builds its schema via `EnsureCreated()` on SQLite in-memory, bypassing the migration path entirely; it would still pass even if the MSSQL/SQLite migrations were missing, which they are. This is the same cross-cutting, cross-story decision (add migrations for all providers, or switch the default `DBProvider` to `postgresql`) already logged against Story 1.1/1.2 — recorded again here since it now blocks a second, independent feature.

- source_spec: `C:\Users\farro\src\k53-guru\_bmad-output\implementation-artifacts\spec-1-3-question-content-model.md`
  summary: `QuestionValidationInterceptor` only re-validates entries where the `Question` itself is tracked as `Added`/`Modified`, reading the invariant off the in-memory `AnswerOptions` navigation — so (a) editing only child `AnswerOption` rows while the parent `Question` entity stays `Unchanged` skips re-validation entirely, and (b) a `Modified` `Question` whose `AnswerOptions` weren't `Include`d in that unit of work would see an empty in-memory collection and incorrectly reject an unrelated field-only update (e.g. editing just `Stem`) as "zero correct answers."
  evidence: Both the blind-hunter and edge-case-hunter review layers independently flagged this from different angles. No current caller can exercise either path today — Epic 2 (authoring/update commands) doesn't exist yet — so this doesn't affect Story 1.3's own tested scope (all 7 matrix rows are fresh-`Add` scenarios), but whoever designs Epic 2's update command needs to either always `Include(q => q.AnswerOptions)` before any `Question` update, or move this check to query the DB directly (tracked + committed rows) rather than trust the in-memory navigation, and additionally scan `Entries<AnswerOption>()` for standalone child-only edits.

- source_spec: `C:\Users\farro\src\k53-guru\_bmad-output\implementation-artifacts\spec-1-3-question-content-model.md`
  summary: `QuestionValidationInterceptor`'s `sign_ref` resolution issues one DB round-trip per `Question` inside `SavingChangesAsync`, an unbatched N+1 pattern for a `SaveChanges` call containing many new `Question`s at once (e.g. a bulk CSV/JSON import).
  evidence: Flagged by the blind-hunter review layer. Not a problem for Story 1.3 itself (no bulk-save caller exists yet), but directly relevant to Epic 2's "Import a question bank via CSV/JSON with reject-on-error validation" story (Story 2.4), which should batch-resolve all `sign_ref`s in one query (e.g. a single `.Where(r => codes.Contains(r.LegislationCode))` lookup) rather than rely on this interceptor firing once per row.

- source_spec: `C:\Users\farro\src\k53-guru\_bmad-output\implementation-artifacts\spec-2-1-author-edit-question.md`
  summary: `AddEditQuestionCommandHandler.ReconcileAnswerOptions` matches submitted options against the existing tracked collection by `Id`; if a caller submits two options sharing the same `Id > 0` (never happens through this story's own UI, but reachable by any other/future caller of the command, e.g. a CSV/JSON import reusing this handler), the second entry silently overwrites the first's field values instead of erroring, and the resulting saved row count no longer matches what was submitted.
  evidence: Flagged by the edge-case-hunter review layer. `ReconcileAnswerOptions` (`AddEditQuestionCommand.cs`) does `item.AnswerOptions.SingleOrDefault(a => a.Id == model.Id)` per submitted item without checking the submitted list itself for duplicate `Id`s first, so a second match against the same existing entity just re-applies different `Text`/`IsCorrect`/`Order` values on top of the first, with no exception and no data-loss signal. Not exploitable through `Questions.razor`/`QuestionFormDialog.razor` today (option `Id`s only ever round-trip from `GetQuestionByIdQuery`, never duplicated client-side), but worth a guard (reject the command if `AnswerOptions` contains duplicate non-zero `Id`s) before Story 2.4's CSV/JSON import reuses this same command with less-trusted input.

- source_spec: `C:\Users\farro\src\k53-guru\_bmad-output\implementation-artifacts\spec-2-1-author-edit-question.md`
  summary: `AddEditQuestionCommandValidator` has no max-length rules for `Stem`/`AnswerOptions[i].Text`, even though `QuestionConfiguration`/`AnswerOptionConfiguration` (Story 1.3) cap them at 2000/1000 chars at the DB level — an overlong submission fails with a raw `DbUpdateException` at `SaveChangesAsync` instead of a clean, field-attributed FluentValidation message, which is what this story's own AC ("the failing field is identified") otherwise guarantees for every other rule.
  evidence: Flagged by the blind-hunter review layer. Low likelihood in practice (`MudTextField` has no client-side length cap either, so this is reachable, just not obviously so) — worth adding `MaximumLength(2000)`/`MaximumLength(1000)` rules alongside the existing `Stem`/`AnswerOptions` rules whenever this validator is next touched (e.g. Story 2.4's import, which will need the same field-level rejection contract for CSV/JSON rows).

- source_spec: `C:\Users\farro\src\k53-guru\_bmad-output\implementation-artifacts\spec-2-2-organise-questions-into-test.md`
  summary: The same missing-max-length-validation gap logged against Story 2.1's `Stem`/`AnswerOptions[i].Text` now also applies to `AddEditTestCommandValidator`'s `Name` (capped at 200 chars by `TestConfiguration`, unvalidated client- or server-side) — an overlong test name fails with a raw `DbUpdateException` instead of a clean message.
  evidence: Flagged by the edge-case-hunter review layer. Same root cause and same low-likelihood-but-reachable profile as the Story 2.1 entry above; worth fixing both together whenever either validator is next touched.

- source_spec: `C:\Users\farro\src\k53-guru\_bmad-output\implementation-artifacts\spec-2-2-organise-questions-into-test.md`
  summary: Nothing enforces that a `Test`'s associated `Question`s actually fall within the `Test`'s own declared `Codes`/`Sections` scope — a `Test` scoped to `Sections = Rules, Codes = Code1` can freely have Signs-only or Code3-only questions attached, undercutting the "coherent, scoped content set" purpose stated in this story's own Intent.
  evidence: Flagged by the blind-hunter review layer. Not required by this story's AC (which only asks that the test "is persisted with its associated questions" and viewable grouped by section/code — it doesn't require scope enforcement), so not fixed here, but likely to matter once Epic 3's composition logic starts trusting a `Test`'s declared scope to pull sittings — worth a validator rule (reject a submitted `QuestionId` whose `Question.Codes`/`Section` falls outside the `Test`'s own `Codes`/`Sections`) when that dependency becomes concrete.

- source_spec: `C:\Users\farro\src\k53-guru\_bmad-output\implementation-artifacts\spec-2-2-organise-questions-into-test.md`
  summary: `TestFormDialog.razor`'s question multi-select (`GetAllQuestionsQuery`) loads every `Question` in the bank into one dropdown with no search, filter, or paging.
  evidence: Flagged by the blind-hunter review layer. Fine at current content volume; will need addressing once Story 2.1/2.4 (authoring + CSV/JSON import) grow the question bank past what's comfortable in a single unfiltered dropdown.

- source_spec: `C:\Users\farro\src\k53-guru\_bmad-output\implementation-artifacts\spec-2-4-import-question-bank.md`
  summary: `ImportQuestionsCommandHandler.ParseCsv` configures CsvHelper with `MissingFieldFound = null` and `BadDataFound = null`, which silences CsvHelper's own structural diagnostics for a genuinely malformed or wrong-header file. Such a file degrades into confusing downstream messages (e.g. a generic "Stem is required" for every row) rather than a clear "this file doesn't match the expected column layout" error.
  evidence: Flagged by the blind-hunter review layer. Chosen deliberately so a file authored against an older/narrower version of the template (e.g. missing the `SignRef` column entirely, or a row with fewer than 4 option slots) still parses leniently rather than throwing hard on read — this permissiveness is desired for the common "template evolves, users' saved copies lag" case. The failure mode only degrades UX for a completely wrong file, it doesn't lose or corrupt data. Worth adding a post-`ReadHeader` check (e.g. confirm at least `Stem`/`Codes` header names are present) if this proves confusing in practice.

- source_spec: `C:\Users\farro\src\k53-guru\_bmad-output\implementation-artifacts\spec-2-4-import-question-bank.md`
  summary: Per-row `Enum.Parse` failures (for `Codes`/`Section`/answer-option booleans) surface the raw CLR exception `.Message` verbatim in the `"Row {position}: {message}"` failure string, which is functionally correct (identifies the row, gives a reason) but not maximally readable (e.g. "Requested value 'xyz' was not found." rather than naming the legal values).
  evidence: Flagged by the blind-hunter review layer. A nicer message would require building a per-field "legal values" lookup table; deferred as a message-quality polish, not a functional defect.

- source_spec: `C:\Users\farro\src\k53-guru\_bmad-output\implementation-artifacts\spec-2-4-import-question-bank.md`
  summary: `ImportQuestionsCommandHandler` has no enforced upper bound on row count or file size, and its CSV/JSON parse loops don't propagate a `CancellationToken`, so a very large file (well beyond the documented "tens to low hundreds of rows" v1 target) ties up the request with no way to cancel it mid-parse.
  evidence: Flagged by both the blind-hunter and edge-case-hunter review layers. Consciously deferred alongside the already-accepted N+1 `sign_ref` resolution (see the Story 1.3 N+1 entry above) — both are volume-scaling concerns out of scope for v1's documented expected import size.

- source_spec: `C:\Users\farro\src\k53-guru\_bmad-output\implementation-artifacts\spec-2-4-import-question-bank.md`
  summary: A CSV row supplying more than 4 answer options (columns beyond `Option4Text`/`Option4Correct`) has its excess data silently dropped with no warning or diagnostic — the row imports "successfully" using only the first 4 options.
  evidence: Flagged by the blind-hunter review layer. The 4-option cap itself is an intentional, documented boundary of CSV's flat row shape (this story's own spec: "CSV's flat row shape caps answer options at 4 via fixed columns"), so a 5th column simply isn't part of the format — but silently discarding data a submitter may have intended to include, with no signal that anything was dropped, is worth a future warning (e.g. a non-blocking note appended to the per-row result) rather than staying silent.

- source_spec: `C:\Users\farro\src\k53-guru\_bmad-output\implementation-artifacts\spec-2-5-restrict-content-management-to-admins.md`
  summary: `AuthorizationBehaviour`'s new `[RequestAuthorize]` attribute allows `AllowMultiple = true` (stacking), but multiple attributes' `Roles` lists are flattened and OR-combined (`requiredRoles.Any(...)`) — diverging from ASP.NET Core's own `[Authorize]` convention, where stacked attributes are ANDed together (each must pass independently) while the `Roles` list *within* one attribute is ORed.
  evidence: Flagged by the edge-case-hunter review layer. Not triggered today — each of the five commands decorated in Story 2.5 carries exactly one `[RequestAuthorize]` attribute — but a future developer stacking two attributes expecting a tightening (AND) effect would instead get a silently widened (OR) requirement. Needs a deliberate design decision (support true AND-across-attributes, or drop `AllowMultiple`) before this attribute is ever stacked on a real command.

- source_spec: `C:\Users\farro\src\k53-guru\_bmad-output\implementation-artifacts\spec-2-5-restrict-content-management-to-admins.md`
  summary: `UserContextLoader` caches the loaded `UserContext` (including `Roles`) in FusionCache for a 1-hour TTL, so a user whose `Admin` role is revoked mid-session can still pass the new `AuthorizationBehaviour` role check — and therefore still author/import/publish content — for up to an hour after the revocation.
  evidence: Flagged by the blind-hunter review layer. Pre-existing infrastructure this story didn't introduce and wasn't asked to touch, but the caching now has direct security relevance it didn't have before Story 2.5 (previously nothing server-side consumed `Roles` for an authorization decision on these commands). Worth a shorter TTL or an explicit cache-bust on role change if this staleness window is ever judged unacceptable.

- source_spec: `C:\Users\farro\src\k53-guru\_bmad-output\implementation-artifacts\spec-2-5-restrict-content-management-to-admins.md`
  summary: The recurring MSSQL migration-drift gap (previously logged against `RoadSign` and `Question`/`AnswerOption`, Stories 1.1-1.3) now has a materially escalated, confirmed impact: the ENTIRE `Application.IntegrationTests` project — all 28 tests, not just ones touching the previously-named entities — fails identically at `OneTimeSetUp` with an EF Core `PendingModelChangesWarning` when `Database.Migrate()` runs against the MSSQL provider, because Epic 2's entities (`Test`/`TestQuestion`/etc.) were added without any corresponding `Migrators.MSSQL`/`Migrators.SqLite` migration. This is the first story to actually add a test to `Application.IntegrationTests` (`AuthorizationTests.cs`, exercising the new `AuthorizationBehaviour` end-to-end through the real DI-wired MediatR pipeline) and discover the whole project is currently non-functional in this environment as a result.
  evidence: Confirmed independently by both the implementation subagent and a direct `git stash` comparison against the story's own `baseline_commit` (`4629f34`) — the identical 28/28 failure exists at baseline, before any Story 2.5 change, and none of Story 2.5's changes touch any entity or migration. Previously this gap was framed as "the substitute SQLite in-memory coverage happens to mask a real migration gap for entities X/Y"; it is now, additionally, "the intended real-pipeline integration-test project cannot run at all," which is a stronger and more actionable form of the same root cause. Resolving it (adding the missing `Migrators.MSSQL`/`Migrators.SqLite` migrations, or switching the default/test `DBProvider` to `postgresql`) remains the same cross-cutting, cross-story decision logged against Stories 1.1-1.3, recorded again here because `AuthorizationTests.cs` (Story 2.5) is currently unable to be verified as passing in any environment that reaches this code path, and depends on this being resolved to ever actually run.
