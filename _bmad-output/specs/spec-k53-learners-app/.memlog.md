---
topic: K53 Learner's License practice app: .NET admin/API + Flutter test-taking client across Code 1/2/3
updated: 2026-08-29T10:13
---

- (decision) slug=k53-learners-app; interactive express distill from rich in-chat brief (no source file)
- (capability) CAP-1: Admin authors multiple-choice tests via Admin Panel that mimic the computerised K53 learner's test format; success=admin can create/publish a test consumable by the API
- (capability) CAP-2: Admin imports existing tests via CSV or JSON; success=a well-formed CSV/JSON becomes a stored test with questions, options, correct answers
- (capability) CAP-3: Road-sign catalog pre-loaded with official legislation codes; success=all official signs present, each linked to its legislation code, referenceable by test questions
- (capability) CAP-4: .NET API serves tests to clients and randomises question selection/order per attempt; success=two attempts of same test yield differently ordered/selected questions
- (capability) CAP-5: Flutter app lets learner take a test scoped to a single code (1/2/3) or any combination incl. all three; success=learner picks code(s) and receives a matching test
- (capability) CAP-6: Test-taking flow simulates the real computerised test closely (structure, scoring, pass/fail result); success=completed attempt returns a per-section score and pass/fail mirroring official rules
- (constraint) System spans a .NET backend (Admin Panel + API) and a Flutter mobile client; app pulls tests from the .NET API
- (constraint) Simulation fidelity to the official computerised K53 learner's test is the primary quality bar
- (constraint) Road signs must be stored with their official legislation codes, not ad-hoc labels
- (constraint) Question selection/order randomised per attempt (no fixed sequence)
- (constraint) Covers three licence codes: Code 1 motorcycle, Code 2 light motor vehicle, Code 3 heavy motor vehicle
- (note) Non-goal: excludes K53 practical/yard/driving test (theory learner's test only)
- (note) Non-goal: not an official/government-certified exam; a practice/simulation tool
- (question) Exact K53 section composition and pass thresholds per code (Rules of the Road / Road Signs / Vehicle Controls question counts, pass marks, time limits) — official values unconfirmed
- (question) Licensing/copyright status of the official question bank content — can questions be reproduced?
- (question) Admin Panel authentication/authorisation model (who can create/import tests)
- (question) Is offline test-taking in scope for the Flutter app, or online-only against the API?
- (question) Language support — English only, or multiple SA official languages (e.g. Afrikaans)?
- (event) Self-Validate Pass 1 (Coherence): PASS — all 6 caps have intent+success, constraints bind, 2 non-goals, testable success signal, IDs unique
- (event) Self-Validate Pass 2 (Preservation): PASS — every brief claim (admin authoring, csv/json import, road-sign legislation codes, API randomisation, single/combined code taking, real-test simulation) landed in a kernel field; no wrapper-only drops
- (event) spec finalized (create): SPEC.md derived, 6 capabilities, 0 companions, 2 assumptions, 5 open questions
- (decision) Admin panel's primary author is a domain expert (ex-examiner), not the product owner; authoring UI + CSV/JSON import both target expert-written content (party-mode: Priya/Oom reframe)
- (constraint) CSV/JSON import rows reference road signs by legislation code (sign_ref); a row whose sign_ref does not resolve against the catalog is rejected on import — no broken/image-missing questions reach a learner
- (constraint) An attempt's question set is snapshotted/frozen at start; admin content edits apply only to subsequent attempts, never mutating a live in-progress attempt
- (decision) Road-sign catalog (from SARTSM/legislation) is the foundational first deliverable — pure legally-safe data entry — that questions reference; treat as prerequisite to question authoring
- (capability) CAP-7: Import validates each row against the sign catalog and question schema, rejecting malformed rows; success=an import with an unresolved sign_ref or missing required field is refused with the offending row identified
- (event) Spec update (party-mode fold-in): added CAP-7, 2 constraints (sign_ref validation, attempt snapshot), 2 decisions (expert-author reframe, catalog-first)
- (direction) Anton: LL1 form allows ticking multiple codes; on the day it is ONE test that is a combination (e.g. Code 1 + Code 3). App must simulate that single combined sitting
- (constraint) A combined test (multiple codes selected, mirroring LL1 form) is ONE unified paper, not concatenated per-code tests: shared rules-of-the-road/signs questions appear once; code-specific questions (e.g. vehicle controls) are drawn per selected code
- (constraint) Question content carries two axes: applicable code(s) AND shared-vs-code-specific; combined-test composition = union of shared questions + code-specific questions per selected code
- (constraint) Scoring on a combined paper tracks pass thresholds per-section (and per code-specific section), not just a grand total; failing any section's minimum fails the whole ticket
- (event) Spec update (party-mode): combined test = single LL1-style sitting; added shared/code-specific axis + per-section scoring constraints; resolves combined-test composition open item
- (constraint) Randomisation is intra-section only: question order is shuffled WITHIN each section, but section order is fixed (Rules of the Road -> Signs -> Vehicle Controls; code-specific blocks in order). Tightens CAP-4's per-attempt randomisation
- (constraint) Test-taking UI presents progress by section (e.g. 'Section 2 of 4'), not a single flat question counter, so long combined papers stay legible
- (event) Spec update (party-mode): added intra-section randomisation + sectioned-progress constraints
- (decision) App has two deliberate modes: Practice mode (warm - review, hints, retry, spaced return) and Test mode (fenced-off faithful simulation - locked, timed, sectioned, no hints). Fidelity lives inside Test mode; the surrounding app is designed for a nervous human
- (capability) CAP-8: Learner can choose Practice mode (immediate feedback, hints, retry, no strict timing) or Test mode (faithful cold simulation); success=Practice reveals correctness/explanation as you go while Test withholds all feedback until a final scored result
- (constraint) The faithful-simulation constraints (timing, no hints, sectioned, per-section scoring, intra-section randomisation) apply to Test mode; Practice mode may relax them to teach
- (event) Spec update (party-mode): added CAP-8 practice-vs-test modes + mode-scope constraint
- (note) K53 structure researched (web sources inconclusive/blocked; figures are widely-published study-site values, NOT gazette-confirmed): 3 sections Rules/Signs/Controls. Signs 30(pass23), Rules 30(pass22) all codes. Controls: Code1 8(pass6) total 68(pass51); Code2/3 28(pass22) total 88(pass67). Per-section pass. Recorded in test-structure.md companion with confidence flag
- (constraint) Per-code question counts, pass marks and time limits are configurable data (not hardcoded); defaults live in test-structure.md companion and must be verified against the official DLTC/computerised test before production
- (decision) Added spec-authored companion test-structure.md (per-code matrix) and referenced it in companions:; resolves the section-composition/pass-threshold open question to 'best-known, pending official confirmation'
- (event) Spec update (party-mode): companion test-structure.md added; open question on per-code structure downgraded to a verification task with recorded defaults
- (direction) Anton supplied authoritative CLLT (Computerised Learner's Licence Testing) description: eNaTIS terminals, biometric login, 1200+ question bank drawn at random, 3 independent cut-off sections, per-code combination grading with partial passes
- (note) CONFIRMED counts as RANGES: Rules 28-30 (pass 22), Signs 28-30 (pass 23), Controls 8-12 (pass 6/8 or 10/12). Not fixed numbers. Updated test-structure.md accordingly
- (decision) SUPERSEDES earlier monolithic-ticket model: a COMBINATION test is graded PER CODE independently, partial passes allowed (pass one code, fail the other; keep the pass, re-test only the failed code). Shared Rules+Signs answered once; separate vehicle-control module per code. Per-section cut-off applies WITHIN each code
- (decision) SUPERSEDES 'any combination incl all three': valid combinations are single code, or Code1+Code2, or Code1+Code3 (motorcycle + a four-wheel code). Code2+Code3 is redundant (Code3 covers both); all-three is not a real scenario
- (constraint) Practice app does NOT replicate the real test's biometric/eNaTIS identity verification (out of scope; cannot and should not simulate fingerprint-against-national-DB)
- (note) Each learner's licence endorsement valid 24 months from issue; official bank exceeds 1200 questions (reinforces original-content bank need + randomisation)
- (event) Spec update (party-mode): CLLT authoritative fold-in; corrected combination grading (per-code partial pass), trimmed valid combos, added biometric non-goal, counts now ranges in companion
- (event) Self-Validate after CLLT fold-in: PASS. 8 caps intact w/ intent+success; combination model now per-code partial-pass (supersedes monolithic ticket); valid combos constrained (1+2,1+3); biometric non-goal added; companion updated to ranges. Preservation: all CLLT source claims landed (bank size, sections, ranges, per-code grading, partial pass, redundancy, biometric, 24mo validity)
- (direction) Anton: NEW req - no auth. On app launch, generate a new profile or link an existing one. Generating creates a UUID the learner must keep safe; pasting that UUID into a freshly installed app re-links the profile. Anonymous, no accounts/passwords/PII
- (capability) CAP-9: Learner establishes identity via a self-custodied UUID - on launch, generate a new profile (issues a UUID) or link existing by pasting a UUID; success=pasting a valid UUID into a fresh install restores that profile's data, with no account, password, or PII collected
- (constraint) Learner identity is an anonymous, self-custodied UUID: no auth, no PII; the UUID is the only key to a profile, so losing it loses access (user is responsible for keeping it safe). Replaces any biometric/account identity for learners
- (event) Spec update (party-mode): added CAP-9 UUID profile + anonymous-identity constraint; refines the biometric non-goal into a positive UUID-linking model
